@model Core.Entities.InvoiceItem

@{
    ViewBag.Title = "Delete Invoice Item";
}
<h2>Delete</h2>


<div>
    <h4>Invoice Item</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Description)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Description)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Price)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Price)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Quantity)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Quantity)
        </dd>
    </dl>

    <h5 class="text-danger">Are you sure you want to delete this Invoice?</h5>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-outline-danger" /> |
            @Html.ActionLink("Cancel", "Index", null, new { invoiceId = Model.InvoiceId }, new { @class = "btn btn-outline-dark" })
        </div>
    }
</div>